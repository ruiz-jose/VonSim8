import { atom } from "jotai";

// Átomo para controlar la animación del texto 'Read' en el bus de control RD
export const showReadBusAnimationAtom = atom(false);
// Átomo para controlar la animación del texto 'Write' en el bus de control WR
export const showWriteBusAnimationAtom = atom(false);
