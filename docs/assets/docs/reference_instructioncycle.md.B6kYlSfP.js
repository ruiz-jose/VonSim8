import{_ as o,c,o as d,ag as a}from"./chunks/framework.BRWBV1zN.js";const M=JSON.parse('{"title":"Ciclo de instrucción","description":"","frontmatter":{},"headers":[],"relativePath":"reference/instructioncycle.md","filePath":"reference/instructioncycle.md","lastUpdated":1755642559000}'),r={name:"reference/instructioncycle.md"};function i(n,e,t,s,l,R){return d(),c("div",null,e[0]||(e[0]=[a('<h1 id="ciclo-de-instruccion" tabindex="-1">Ciclo de instrucción <a class="header-anchor" href="#ciclo-de-instruccion" aria-label="Permalink to &quot;Ciclo de instrucción&quot;">​</a></h1><p>Este apartado describe en detalle el proceso llevado a cabo por la Unidad de Control (UC) para ejecutar cada instrucción, destacando los siguientes aspectos:</p><ul><li>Secuencia de pasos.</li><li>Registros involucrados en cada etapa del proceso.</li><li>Uso de los buses de datos, direcciones y control.</li><li>Señales de control generadas.</li></ul><p>El ciclo de la instrucción se define como la secuencia de microoperaciones que tienen lugar durante la ejecución de una instrucción en el sistema. Estas operaciones se describen utilizando un lenguaje de transferencia entre registros, de la forma:</p><p><code>destino</code> ← <code>origen</code></p><p>La visualización del ciclo de instrucción se basa en el modelo RTL (Register Transfer Level), mostrando el desplazamiento de datos y las señales de control en cada fase. Esto facilita la comprensión de las microoperaciones internas y la interacción entre los componentes del procesador.</p><h2 id="etapa-de-captacion" tabindex="-1">Etapa de Captación: <a class="header-anchor" href="#etapa-de-captacion" aria-label="Permalink to &quot;Etapa de Captación:&quot;">​</a></h2><p>En esta etapa, común a todas las instrucciones, consiste en la lectura de la instrucción desde la memoria.</p><ol><li><strong><code>MAR</code> $\\leftarrow$ <code>IP</code></strong>: La Unidad de Control (UC) transfiere el contenido del registro puntero de instrucciones (<code>IP</code>) al registro de direcciones de memoria (<code>MAR</code>). Este paso prepara el sistema para acceder a la posición de memoria donde se encuentra la próxima instrucción.</li><li><strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code> ; <code>IP</code> $\\leftarrow$ <code>IP</code> + 1</strong>: La UC activa la señal de lectura para obtener el contenido de la memoria en la dirección especificada por el (<code>MAR</code>). El dato leído se transfiere al registro de datos de memoria (<code>MBR</code>) mediante el bus de datos. Simultáneamente, el registro (<code>IP</code>) se incrementa en uno, apuntando así a la siguiente instrucción en memoria u operando de la instrucción vigente.</li><li><strong><code>IR</code> $\\leftarrow$ <code>MBR</code></strong>: Finalmente, el contenido del registro de datos de memoria (<code>MBR</code>) se copia en el registro de instrucciones (<code>IR</code>). Con este paso, la instrucción ha sido captada y está lista para ser decodificada y ejecutada.</li></ol><p>La etapa de captación tiene 3 ciclos para todas las instrucciones.</p><h3 id="etapa-de-ejecucion" tabindex="-1">Etapa de Ejecución: <a class="header-anchor" href="#etapa-de-ejecucion" aria-label="Permalink to &quot;Etapa de Ejecución:&quot;">​</a></h3><p>En esta etapa, las operaciones específicas dependen del tipo de instrucción. A continuación, se describen algunos casos representativos:</p><p>Instruccion de transferencia de datos MOV</p><h4 id="destino-registro" tabindex="-1">Destino registro <a class="header-anchor" href="#destino-registro" aria-label="Permalink to &quot;Destino registro&quot;">​</a></h4><ul><li><p>MOV <code>Rx</code>, <code>Ry</code> (Entre registros) 4. <strong><code>Rx</code> $\\leftarrow$ <code>Ry</code></strong>: El valor almacenado en Ry se copia en el registro Rx.</p></li><li><p>MOV <code>Rx</code>, <code>[Dirección]</code> (Directo) 4. <strong><code>MAR</code> $\\leftarrow$ <code>IP</code></strong>: El contenido de IP se transfiere al registro MAR. 5. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code>; <code>IP</code> $\\leftarrow$ <code>IP</code> + 1</strong>: Se lee el dato de la memoria en la dirección indicada por MAR y se almacena en MBR; luego, IP se incrementa en uno.<br> 6. <strong><code>MAR</code> $\\leftarrow$ <code>MBR</code></strong>: El valor de MBR se transfiere a MAR. 7. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code></strong>: Se lee el dato de la memoria en la nueva dirección de MAR y se almacena en MBR. 8. <strong><code>Rx</code> $\\leftarrow$ <code>MBR</code></strong>: Finalmente, el contenido de MBR se copia en el registro Rx.</p></li><li><p>MOV <code>Rx</code>, <code>Dato</code> (Inmediato) 4. 5. Se obtiene el operando de la instrucción. 6. <strong><code>Rx</code> $\\leftarrow$ <code>MBR</code></strong>: El contenido de MBR se transfiere al registro Rx.</p></li><li><p>MOV <code>Rx</code>, <code>[BL]</code> (Indirecto) 4. <strong><code>MAR</code> $\\leftarrow$ <code>BL</code></strong>: El contenido de BL se transfiere al registro MAR. 5. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code></strong>: Se lee el dato de la memoria en la dirección indicada por MAR y se almacena en MBR. 6. <strong><code>Rx</code> $\\leftarrow$ <code>MBR</code></strong>: Finalmente, el contenido de MBR se copia en el registro Rx.</p><h4 id="destino-memoria" tabindex="-1">Destino Memoria <a class="header-anchor" href="#destino-memoria" aria-label="Permalink to &quot;Destino Memoria&quot;">​</a></h4></li><li><p>MOV <code>[Dirección]</code>, <code>Ry</code> (Directo)<br> 4. 5. Se obtiene el operando de la instrucción. 6. <strong><code>MAR</code> $\\leftarrow$ <code>MBR</code></strong>: El valor de MBR se transfiere al registro MAR. 7. <strong><code>MBR</code> $\\leftarrow$ <code>Ry</code></strong>: El contenido de Ry se copia en MBR. 8. <strong><code>write(Memoria[MAR])</code> $\\leftarrow$ <code>MBR</code></strong>: Se escribe el valor de MBR en la posición de memoria indicada por MAR.</p></li><li><p>MOV <code>[BL]</code>, <code>Ry</code> (Indirecto)<br> 4. <strong><code>MAR</code> $\\leftarrow$ <code>BL</code></strong>: El contenido de BL se transfiere al registro MAR. 5. <strong><code>MBR</code> $\\leftarrow$ <code>Ry</code></strong>: El contenido de Ry se copia en MBR. 6. <strong><code>write(Memoria[MAR])</code> $\\leftarrow$ <code>MBR</code></strong>: Se escribe el valor de MBR en la posición de memoria indicada por MAR.</p></li><li><p>MOV <code>[Dirección]</code>, <code>Dato</code> (Inmediato)<br> 4. 5. Se obtiene el operando de la instrucción. 6. <strong><code>MAR</code> $\\leftarrow$ <code>IP</code>;<code>ri</code> $\\leftarrow$ <code>MBR</code></strong>: El contenido de IP se transfiere al registro MAR y simultaneamente se transfiere el valor de MBR a un registro intermedio (ri) .<br> 7. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code>; <code>IP</code> $\\leftarrow$ <code>IP</code> + 1</strong>: Se lee el dato de la memoria en la dirección indicada por MAR y se almacena en MBR; luego, IP se incrementa en uno.<br> 8. <strong><code>MAR</code> $\\leftarrow$ <code>ri</code></strong>: El contenido de ri se transfiere al registro MAR. 9. <strong><code>write(Memoria[MAR])</code> $\\leftarrow$ <code>MBR</code></strong>: Se escribe el valor de MBR en la posición de memoria indicada por MAR.</p></li><li><p>MOV <code>[BL]</code>, <code>Dato</code> (Inmediato)<br> 4. 5. Se obtiene el operando de la instrucción. 6. <strong><code>MAR</code> $\\leftarrow$ <code>BL</code></strong>: El contenido de BL se transfiere al registro MAR. 7. <strong><code>write(Memoria[MAR])</code> $\\leftarrow$ <code>MBR</code></strong>: Se escribe el valor de MBR en la posición de memoria indicada por MAR.</p></li></ul><ul><li><p>ADD <code>Rx</code>, <code>[Dirección]</code> (Sumar a registro) 1, 2, 3, 4. Igual que MOV <code>Rx</code>, <code>[Dirección]</code>. 5. <strong><code>Rx</code> ← <code>Rx</code> + <code>MBR</code></strong>: La Unidad Aritmético-Lógica (ALU) realiza la suma entre <code>Rx</code> y <code>MBR</code>, almacenando el resultado en <code>Rx</code>. El Registro de Estado <code>RS</code> se actualiza con los indicadores correspondientes.</p></li><li><p>SUB <code>[Dirección]</code>, <code>Ry</code> (Restar a memoria) 1, 2, 3, 4. Igual que MOV <code>Rx</code>, <code>[Dirección]</code>. 5. <strong><code>MBR</code> ← <code>Ry</code> - <code>MBR</code></strong>: La ALU resta el contenido de <code>MBR</code> al de <code>Ry</code>, almacenando el resultado en <code>MBR</code>. El <code>RS</code> se actualiza. 6. <strong><code>write(Memoria[MAR])</code> ← <code>MBR</code></strong>: El contenido de <code>MBR</code> se escribe en memoria en la dirección apuntada por el <code>MAR</code>.</p></li><li><p>CMP <code>Rx</code>, <code>[Dirección]</code> (Comparar a registro) 1, 2, 3, 4. Igual que MOV <code>Rx</code>, <code>[Dirección]</code>. 5. <strong><code>Rx</code> - <code>MBR</code></strong>: La ALU realiza la resta entre el contenido de <code>Rx</code> y <code>MBR</code>. Aunque el resultado no se almacena, el <code>RS</code> se actualiza con los indicadores de comparación.</p></li><li><p>JMP <code>Dirección</code> (Salto incondicional) 1, 2. Igual que MOV <code>Rx</code>, <code>[Dirección]</code>. 3. <strong><code>IP</code> ← <code>MBR</code></strong>: El contenido del <code>MBR</code> se transfiere al registro <code>IP</code>, estableciendo la nueva dirección de ejecución.</p></li></ul>',16)]))}const u=o(r,[["render",i]]);export{M as __pageData,u as default};
