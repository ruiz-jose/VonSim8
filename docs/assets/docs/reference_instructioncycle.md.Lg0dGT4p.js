import{_ as o,c,o as d,ag as a}from"./chunks/framework.BRWBV1zN.js";const R=JSON.parse('{"title":"Ciclo de instrucción","description":"","frontmatter":{},"headers":[],"relativePath":"reference/instructioncycle.md","filePath":"reference/instructioncycle.md","lastUpdated":1753792711000}'),i={name:"reference/instructioncycle.md"};function n(r,e,t,l,s,p){return d(),c("div",null,e[0]||(e[0]=[a('<h1 id="ciclo-de-instruccion" tabindex="-1">Ciclo de instrucción <a class="header-anchor" href="#ciclo-de-instruccion" aria-label="Permalink to &quot;Ciclo de instrucción&quot;">​</a></h1><p>Este apartado describe en detalle el proceso llevado a cabo por la Unidad de Control (UC) para ejecutar cada instrucción, destacando los siguientes aspectos:</p><ul><li>Secuencia de pasos.</li><li>Registros involucrados en cada etapa del proceso.</li><li>Uso de los buses de datos, direcciones y control.</li><li>Señales de control generadas.</li></ul><p>El ciclo de la instrucción se define como la secuencia de microoperaciones que tienen lugar durante la ejecución de una instrucción en el sistema. Estas operaciones se describen utilizando un lenguaje de transferencia entre registros, de la forma:</p><p><code>destino</code> ← <code>origen</code></p><p>La visualización del ciclo de instrucción se basa en el modelo RTL (Register Transfer Level), mostrando el desplazamiento de datos y las señales de control en cada fase. Esto facilita la comprensión de las microoperaciones internas y la interacción entre los componentes del procesador.</p><h2 id="etapa-de-captacion" tabindex="-1">Etapa de Captación: <a class="header-anchor" href="#etapa-de-captacion" aria-label="Permalink to &quot;Etapa de Captación:&quot;">​</a></h2><p>En esta etapa, común a todas las instrucciones, se realiza la lectura de la instrucción desde la memoria.</p><ol><li><strong><code>MAR</code> ← <code>IP</code></strong>: El contenido del registro puntero de instrucciones <code>IP</code> se transfiere al registro de direcciones de memoria <code>MAR</code>. La UC genera la señal necesaria para seleccionar el valor del <code>IP</code> y copiarlo en el <code>MAR</code>.</li><li><strong><code>MDR</code> ← <code>read(Memoria[MAR])</code> ; <code>IP</code> ← <code>IP</code> + 1</strong>: La UC activa la señal de lectura (read) hacia la memoria, utilizando el valor del <code>MAR</code> como dirección. El dato leído se transfiere al Registro de Datos de Memoria <code>MBR</code> a través del bus de datos. Simultáneamente, el <code>IP</code> se incrementa en 1 para apuntar al siguiente byte.</li><li><strong><code>IR</code> ← <code>MBR</code></strong>: El contenido del <code>MBR</code> se transfiere al Registro de Instrucciones <code>IR</code>, completando la etapa de captación.</li></ol><h2 id="etapa-de-ejecucion" tabindex="-1">Etapa de Ejecución: <a class="header-anchor" href="#etapa-de-ejecucion" aria-label="Permalink to &quot;Etapa de Ejecución:&quot;">​</a></h2><p>En esta etapa, las operaciones específicas dependen del tipo de instrucción. A continuación, se describen algunos casos representativos:</p><ul><li><p>MOV <code>Rx</code>, <code>Ry</code> (Copiar entre registros)</p><ol><li><strong><code>Rx</code> ← <code>Ry</code></strong>: El contenido del registro <code>Ry</code> se copia en el registro <code>Rx</code>.</li></ol></li><li><p>MOV <code>Rx</code>, <code>[Dirección]</code> (Cargar a registro)</p><ol><li><strong><code>MAR</code> ← <code>IP</code></strong>: El valor del <code>IP</code> se transfiere a <code>MAR</code>.</li><li><strong><code>MBR</code> ← <code>read(Memoria[MAR])</code>; <code>IP</code> ← <code>IP</code> + 1</strong>: Se lee (read) de memoria el contenido de la dirección indicada por <code>MAR</code> y se almacena en <code>MBR</code>.Simultáneamente, el <code>IP</code> se incrementa.</li><li><strong><code>MAR</code> ← <code>MBR</code></strong>: El contenido de <code>MBR</code> se transfiere a <code>MAR</code>.</li><li><strong><code>MBR</code> ← <code>read(Memoria[MAR])</code></strong>: Se lee de memoria el contenido de la dirección indicada por <code>MAR</code> y se almacena en <code>MBR</code>.</li><li><strong><code>Rx</code> ← <code>MBR</code></strong>: El contenido del <code>MBR</code> se copia al registro <code>Rx</code>.</li></ol></li><li><p>MOV <code>[Dirección]</code>, <code>Ry</code> (Almacenar en memoria)<br> 1, 2, 3. Igual que MOV <code>Rx</code>, <code>[Dirección]</code>. 4. <strong><code>MBR</code> ← <code>Ry</code></strong>: El contenido de <code>Ry</code> se transfiere a <code>MBR</code>. 5. <strong><code>write(Memoria[MAR])</code> ← <code>MBR</code></strong>: El contenido de <code>MBR</code> se escribe (write) en memoria en la dirección apuntada por el <code>MAR</code>.</p></li><li><p>ADD <code>Rx</code>, <code>[Dirección]</code> (Sumar a registro) 1, 2, 3, 4. Igual que MOV <code>Rx</code>, <code>[Dirección]</code>. 5. <strong><code>Rx</code> ← <code>Rx</code> + <code>MBR</code></strong>: La Unidad Aritmético-Lógica (ALU) realiza la suma entre <code>Rx</code> y <code>MBR</code>, almacenando el resultado en <code>Rx</code>. El Registro de Estado <code>RS</code> se actualiza con los indicadores correspondientes.</p></li><li><p>SUB <code>[Dirección]</code>, <code>Ry</code> (Restar a memoria) 1, 2, 3, 4. Igual que MOV <code>Rx</code>, <code>[Dirección]</code>. 5. <strong><code>MBR</code> ← <code>Ry</code> - <code>MBR</code></strong>: La ALU resta el contenido de <code>MBR</code> al de <code>Ry</code>, almacenando el resultado en <code>MBR</code>. El <code>RS</code> se actualiza. 6. <strong><code>write(Memoria[MAR])</code> ← <code>MBR</code></strong>: El contenido de <code>MBR</code> se escribe en memoria en la dirección apuntada por el <code>MAR</code>.</p></li><li><p>CMP <code>Rx</code>, <code>[Dirección]</code> (Comparar a registro) 1, 2, 3, 4. Igual que MOV <code>Rx</code>, <code>[Dirección]</code>. 5. <strong><code>Rx</code> - <code>MBR</code></strong>: La ALU realiza la resta entre el contenido de <code>Rx</code> y <code>MBR</code>. Aunque el resultado no se almacena, el <code>RS</code> se actualiza con los indicadores de comparación.</p></li><li><p>JMP <code>Dirección</code> (Salto incondicional) 1, 2. Igual que MOV <code>Rx</code>, <code>[Dirección]</code>. 3. <strong><code>IP</code> ← <code>MBR</code></strong>: El contenido del <code>MBR</code> se transfiere al registro <code>IP</code>, estableciendo la nueva dirección de ejecución.</p></li></ul>',12)]))}const m=o(i,[["render",n]]);export{R as __pageData,m as default};
