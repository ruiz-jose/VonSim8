import{_ as o,c,o as r,ag as d}from"./chunks/framework.BRWBV1zN.js";const p=JSON.parse('{"title":"Ciclo de la instrucción","description":"","frontmatter":{},"headers":[],"relativePath":"reference/instructioncycle.md","filePath":"reference/instructioncycle.md","lastUpdated":1757295298000}'),a={name:"reference/instructioncycle.md"};function n(t,e,i,s,l,g){return r(),c("div",null,e[0]||(e[0]=[d('<h1 id="ciclo-de-la-instruccion" tabindex="-1">Ciclo de la instrucción <a class="header-anchor" href="#ciclo-de-la-instruccion" aria-label="Permalink to &quot;Ciclo de la instrucción&quot;">​</a></h1><p>El ciclo de la instrucción es la secuencia de pasos que realiza la Unidad de Control (UC) para ejecutar cada instrucción de un programa. Este proceso es fundamental para el funcionamiento de cualquier computadora, ya que involucra elementos clave como registros, buses de datos, direcciones y señales de control generadas por la UC.</p><p>Las microoperaciones que lo componen se expresan mediante la notación de transferencia entre registros: <code>destino</code> $\\leftarrow$ <code>origen</code>.</p><p>La Figura @ref(fig:flujoCicloInstruccion) ilustra el flujo general del ciclo, el cual se divide en dos etapas principales: captación (fetch) y ejecución.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>knitr::include_graphics(path = &quot;images/cicloinstruccion3.png&quot;)</span></span></code></pre></div><h2 id="etapa-1-captacion" tabindex="-1">Etapa 1: Captación <a class="header-anchor" href="#etapa-1-captacion" aria-label="Permalink to &quot;Etapa 1: Captación&quot;">​</a></h2><p>Esta etapa es igual para todas las instrucciones. Su objetivo es leer la instrucción desde la memoria y cargarla en el <strong>Registro de Instrucciones</strong> (<code>IR</code>). Consta de tres microoperaciones:</p><ol><li><p><strong><code>MAR</code> $\\leftarrow$ <code>IP</code></strong> La UC transfiere al <strong>Registro de Direcciones de Memoria</strong> (<code>MAR</code>) la dirección de la próxima instrucción, almacenada en el <strong>Puntero de Instrucción</strong> (<code>IP</code>) .</p></li><li><p><strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code> | <code>IP</code> $\\leftarrow$ <code>IP</code> + 1</strong> La UC activa la señal de lectura para leer la instrucción ubicada en la dirección contenida en el <code>MAR</code>. El valor leído se guarda en el <strong>Registro de Datos de Memoria</strong> (<code>MBR</code>) y, al mismo tiempo, el <code>IP</code> se incrementa para apuntar a la siguiente instrucción u operando.</p></li><li><p><strong><code>IR</code> $\\leftarrow$ <code>MBR</code></strong> El contenido del <code>MBR</code> se transfiere al <code>IR</code>, dejando la instrucción lista para ser decodificada y ejecutada.</p></li></ol><h2 id="etapa-2-ejecucion" tabindex="-1">Etapa 2: Ejecución <a class="header-anchor" href="#etapa-2-ejecucion" aria-label="Permalink to &quot;Etapa 2: Ejecución&quot;">​</a></h2><p>En esta etapa, el <strong>decodificador de instrucciones</strong> interpreta el valor en el registro <code>IR</code>. A partir del código de operación, lee las microinstrucciones necesarias en la <strong>memoria de control</strong> para determinar el tipo de instrucción, la cantidad de operandos y el modo de direccionamiento.</p><p>Luego, estas microinstrucciones se envían a <strong>secuenciador</strong>, que genera las señales de control precisas para ejecutar la operación.</p><p>A continuación se detallan las instrucciones más comunes:</p><h3 id="instrucciones-con-dos-operandos-mov-add-sub-y-cmp" tabindex="-1">Instrucciones con dos operandos <code>MOV</code>, <code>ADD</code>, <code>SUB</code> y <code>CMP</code>: <a class="header-anchor" href="#instrucciones-con-dos-operandos-mov-add-sub-y-cmp" aria-label="Permalink to &quot;Instrucciones con dos operandos `MOV`, `ADD`, `SUB` y `CMP`:&quot;">​</a></h3><ul><li><p><strong>Destino en registro (<code>Rx</code>)</strong></p><ul><li><strong>Modo entre registros (<code>Rx</code>, <code>Ry</code>)</strong> 4. La ejecución se realiza en un solo paso: - MOV: <strong><code>Rx</code> $\\leftarrow$ <code>Ry</code></strong> - ADD: <strong><code>Rx</code> $\\leftarrow$ <code>Rx</code> + <code>Ry</code> | update(Flags)</strong> - SUB: <strong><code>Rx</code> $\\leftarrow$ <code>Rx</code> - <code>Ry</code> | update(Flags)</strong> - CMP: <strong><code>Rx</code> - <code>Ry</code> | update(Flags)</strong> (solo actualiza flags, no guarda el resultado)</li><li><strong>Modo directo (<code>Rx</code>, <code>[Dirección]</code>)</strong> 4. <strong><code>MAR</code> $\\leftarrow$ <code>IP</code></strong> – Obtener dirección del operando fuente. 5. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code> | <code>IP</code> $\\leftarrow$ <code>IP</code> + 1</strong> – Leer la dirección desde memoria e incrementar IP. 6. <strong><code>MAR</code> $\\leftarrow$ <code>MBR</code></strong> – Transferir la dirección al MAR. 7. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code></strong> – Obtener el dato. 8. Ejecutar la operación: - MOV: <strong><code>Rx</code> $\\leftarrow$ <code>MBR</code></strong> - ADD: <strong><code>Rx</code> $\\leftarrow$ <code>Rx</code> + <code>MBR</code> | update(Flags)</strong> - SUB: <strong><code>Rx</code> $\\leftarrow$ <code>Rx</code> - <code>MBR</code> | update(Flags)</strong> - CMP: <strong><code>Rx</code> - <code>MBR</code> | update(Flags)</strong></li><li><strong>Modo inmediato (<code>Rx</code>, <code>Dato</code>)</strong> 4. <strong><code>MAR</code> $\\leftarrow$ <code>IP</code></strong> – Obtener dirección del dato inmediato. 5. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code> | <code>IP</code> $\\leftarrow$ <code>IP</code> + 1</strong> – Leer el dato e incrementar IP. 6. Ejecutar la operación (igual que en el caso anterior).</li><li><strong>Modo indirecto (<code>Rx</code>, <code>[BL]</code>)</strong> 4. <strong><code>MAR</code> $\\leftarrow$ <code>BL</code></strong> – Obtener dirección del dato desde el registro BL. 5. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code></strong> – Leer el dato. 6. Ejecutar la operación (igual que en el caso anterior).</li></ul></li><li><p><strong>Destino en memoria (<code>[Dirección]</code> o <code>[BL]</code>)</strong> En este caso, el resultado de la operación se almacena en una dirección de memoria especificada en la instrucción o indicada por el contenido del registro BL.</p><ul><li><strong>Modo Directo (<code>[Dirección]</code>, <code>Ry</code>)</strong> 4. <strong><code>MAR</code> $\\leftarrow$ <code>IP</code></strong> – Obtener dirección destino. 5. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code> | <code>IP</code> $\\leftarrow$ <code>IP</code> + 1</strong> – Leer la dirección e incrementar IP. 6. <strong><code>MAR</code> $\\leftarrow$ <code>MBR</code></strong> – Transferir dirección a MAR. Según la instrucción: <ul><li>MOV: 7. <strong><code>MBR</code> $\\leftarrow$ <code>Ry</code></strong> – Copiar Ry al MBR. 8. <strong><code>write(Memoria[MAR])</code> $\\leftarrow$ <code>MBR</code></strong> – Escribir en memoria.</li><li>ADD, SUB, CMP: 7. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code></strong> – Leer el dato. 8. Ejecutar la operación: - ADD: <strong><code>MBR</code> $\\leftarrow$ <code>MBR</code> + <code>Ry</code> | update(Flags)</strong> - SUB: <strong><code>MBR</code> $\\leftarrow$ <code>MBR</code> - <code>Ry</code> | update(Flags)</strong> - CMP: <strong><code>MBR</code> - <code>Ry</code> | update(Flags)</strong> 9. Si es <code>ADD</code> o <code>SUB</code>: <strong><code>write(Memoria[MAR])</code> $\\leftarrow$ <code>MBR</code></strong> – Escribir en memoria.</li></ul></li><li><strong>Modo Indirecto (<code>[BL]</code>, <code>Ry</code>)</strong> 4. <strong><code>MAR</code> $\\leftarrow$ <code>BL</code></strong> – Transferir dirección de destino (en BL) a MAR. Según la instrucción: <ul><li>MOV: 5. <strong><code>MBR</code> $\\leftarrow$ <code>Ry</code></strong> – Copiar Ry al MBR. 6. <strong><code>write(Memoria[MAR])</code> $\\leftarrow$ <code>MBR</code></strong> – Escribir en memoria.</li><li>ADD, SUB, CMP: 5. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code></strong> – Leer el dato. 6. Ejecutar la operación (igual que en el caso anterior). 7. Si es <code>ADD</code> o <code>SUB</code>: <strong><code>write(Memoria[MAR])</code> $\\leftarrow$ <code>MBR</code></strong> – Escribir en memoria.</li></ul></li><li><strong>Modo Directo-Inmediato (<code>[Dirección]</code>, <code>Dato</code>)</strong> 4. <strong><code>MAR</code> $\\leftarrow$ <code>IP</code></strong> – Obtener dirección destino. 5. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code> | <code>IP</code> $\\leftarrow$ <code>IP</code> + 1</strong> – Leer dirección e incrementar IP. 6. <strong><code>MAR</code> $\\leftarrow$ <code>IP</code>; <code>ri</code> $\\leftarrow$ <code>MBR</code></strong> – Preparar para leer el dato y guardar la dirección destino en un registro intermedio (ri). 7. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code> | <code>IP</code> $\\leftarrow$ <code>IP</code> + 1</strong> – Leer dato e incrementar IP. Según la instrucción: <ul><li>MOV: 8. <strong><code>MAR</code> $\\leftarrow$ <code>ri</code></strong> – Copiar dirección destino. 9. <strong><code>write(Memoria[MAR])</code> $\\leftarrow$ <code>MBR</code></strong> – Escribir en memoria.</li><li>ADD, SUB, CMP: 8. <strong><code>MAR</code> $\\leftarrow$ <code>ri</code> | <code>id</code> $\\leftarrow$ <code>MBR</code></strong> – Cargar dirección destino y guardar el valor inmediato en id. 9. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code></strong> – Leer el valor actual de destino. 10. Ejecutar la operación: - ADD: <strong><code>MBR</code> $\\leftarrow$ <code>MBR</code> + <code>id</code> | update(Flags)</strong> - SUB: <strong><code>MBR</code> $\\leftarrow$ <code>MBR</code> - <code>id</code> | update(Flags)</strong> - CMP: <strong><code>MBR</code> - <code>id</code> | update(Flags)</strong> 11. Si es <code>ADD</code> o <code>SUB</code>: <strong><code>write(Memoria[MAR])</code> $\\leftarrow$ <code>MBR</code></strong> – Escribir en memoria.</li></ul></li><li><strong>Modo Indirecto-Inmediato (<code>[BL]</code>, <code>Dato</code>)</strong> 4. <strong><code>MAR</code> $\\leftarrow$ <code>IP</code></strong> – Obtener dirección del dato inmediato. 5. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code> | <code>IP</code> $\\leftarrow$ <code>IP</code> + 1</strong> – Leer dato e incrementar IP. Según la instrucción: <ul><li>MOV: 6. <strong><code>MAR</code> $\\leftarrow$ <code>BL</code></strong> – Copiar dirección de destino. 7. <strong><code>write(Memoria[MAR])</code> $\\leftarrow$ <code>MBR</code></strong></li><li>ADD, SUB, CMP: 6. <strong><code>MAR</code> $\\leftarrow$ <code>BL</code> | <code>id</code> $\\leftarrow$ <code>MBR</code></strong> – Cargar la dirección destino y guardar el valor inmediato en id. 7. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code></strong> – Leer el valor actual de destino. 8. Ejecutar la operación: - ADD: <strong><code>MBR</code> $\\leftarrow$ <code>MBR</code> + <code>id</code> | update(Flags)</strong> - SUB: <strong><code>MBR</code> $\\leftarrow$ <code>MBR</code> - <code>id</code> | update(Flags)</strong> - CMP: <strong><code>MBR</code> - <code>id</code> | update(Flags)</strong> 9. Si es <code>ADD</code> o <code>SUB</code>: <strong><code>write(Memoria[MAR])</code> $\\leftarrow$ <code>MBR</code></strong> – Escribir en memoria.</li></ul></li></ul></li></ul><h3 id="instrucciones-con-un-operando-jmp-y-jxx" tabindex="-1">Instrucciones con un operando <code>JMP</code> y <code>Jxx</code>: <a class="header-anchor" href="#instrucciones-con-un-operando-jmp-y-jxx" aria-label="Permalink to &quot;Instrucciones con un operando `JMP` y `Jxx`:&quot;">​</a></h3><ul><li><p><strong>Salto a (<code>Dirección</code>)</strong> Tanto incondicional <code>JMP</code> como condicionales <code>Jxx</code> tienen estos pasos: 4. <strong><code>MAR</code> $\\leftarrow$ <code>IP</code></strong> – Obtener la dirección del salto. 5. <strong><code>MBR</code> $\\leftarrow$ <code>read(Memoria[MAR])</code>; <code>IP</code> $\\leftarrow$ <code>IP</code> + 1</strong> – Leer la dirección de destino e incrementar IP.</p><p>Según la instrucción:</p><ul><li><p>JMP: 6. <strong><code>IP</code> $\\leftarrow$ <code>MBR</code></strong></p></li><li><p>Jxx: 6. <strong><code>IP</code> $\\leftarrow$ <code>MBR</code></strong> si se cumple la condición del flag <code>xx</code>; en caso contrario, continúa con la siguiente instrucción.</p></li></ul></li></ul><h3 id="instrucciones-sin-operandos" tabindex="-1">Instrucciones sin operandos <a class="header-anchor" href="#instrucciones-sin-operandos" aria-label="Permalink to &quot;Instrucciones sin operandos&quot;">​</a></h3><ul><li>HLT: 4. Detiene la ejecución de la CPU.</li></ul>',18)]))}const M=o(a,[["render",n]]);export{p as __pageData,M as default};
