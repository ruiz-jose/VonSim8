{
  "name": "@vonsim/scripts",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "node src/build.js",
    "lint": "tsc --noEmit && eslint --ignore-path .gitignore .",
    "lint:fix": "tsc --noEmit && eslint --ignore-path .gitignore . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "echo 'No tests for scripts package' && exit 0",
    "test:coverage": "echo 'No tests for scripts package' && exit 0"
  },
  "dependencies": {
    "execa": "^8.0.1",
    "glob": "^10.3.10"
  },
  "devDependencies": {
    "@types/node": "^20.10.8",
    "@vonsim/tsconfig": "workspace:^",
    "eslint": "^9.34.0",
    "eslint-config-vonsim": "workspace:^",
    "prettier": "^3.5.3",
    "typescript": "~5.3.3"
  }
}
